# ── Voice pipeline ─────────────────────────────────────────
pipecat-ai[groq,silero]>=1.0

# ── LLM + STT ──────────────────────────────────────────────
groq>=0.11

# ── MCP (KB / tools server) ────────────────────────────────
mcp>=1.2

# ── Knowledge base / hybrid search ─────────────────────────
chromadb>=0.6
sentence-transformers>=3.0
rank-bm25>=0.2

# ── HTTP clients ───────────────────────────────────────────
aiohttp>=3.9
httpx>=0.27

# ── Config + logging ───────────────────────────────────────
python-dotenv>=1.0
loguru>=0.7

# ── Dev / test ─────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
